<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybtis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.stackoverflow.hr.dao.UserRoleDAO">

    <resultMap id="userRoleMap" type="userRole">
        <id property="id" column="id"></id>
        <result property="userId" column="user_id"></result>
        <result property="roleId" column="role_id"></result>
    </resultMap>

    <select id="selectByPage" resultMap="userRoleMap" resultType="userRole">
        select * from user_role
        <where>
            <if test="searchMap != null">
                <if test="searchMap.userId != null">
                    user_id = #{searchMap.userId}
                </if>
                <if test="searchMap.roleId != null">
                    and role_id = #{searchMap.roleId}
                </if>
            </if>
        </where>
        order by #{order} #{sort} limit #{offset},#{limit}
    </select>

    <select id="selectByCondition" resultMap="userRoleMap" resultType="userRole">
        select * from user_role
        <where>
            <if test="userId != null">
                user_id = #{userId}
            </if>
            <if test="roleId != null">
                and role_id = #{roleId}
            </if>
        </where>
    </select>

    <select id="select" resultMap="userRoleMap" resultType="userRole">
        select * from user_role where id = #{id}
    </select>

    <insert id="insert" parameterType="userRole">
        insert into user_role (id,user_id,role_id)
        values (#{id},#{userId},#{roleId})
    </insert>

    <insert id="batchInsert" parameterType="list">
        insert into user_role (id,user_id,role_id)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id},#{item.userId},#{item.roleId})
        </foreach>
    </insert>

    <delete id="delete" parameterType="string">
        delete from user_role where id = #{id}
    </delete>

    <delete id="batchDelete" parameterType="list">
        delete from user_role where id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="update" parameterType="userRole">
        update user_role
        <set>
            <if test="userId!=null">
                user_id=#{userId},
            </if>
            <if test="roleId!=null">
                role_id=#{roleId}
            </if>
        </set>
        where id=#{id}
    </update>

    <update id="batchUpdate" parameterType="list">
        <foreach collection="list" item="item" open="" close="" separator=";">
            update user_role
            <set>
                <if test="item.userId!=null">
                    user_id=#{item.userId},
                </if>
                <if test="item.roleId!=null">
                    role_id=#{item.roleId}
                </if>
            </set>
            where id = #{item.id}
        </foreach>
    </update>

</mapper>